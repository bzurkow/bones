{
  "name": "email-templates",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate-tokens": "node scripts/generate-tokens.js",
    "dev": "yarn generate-tokens && email dev --port 3010",
    "build": "yarn generate-tokens && tsc -p tsconfig.build.json",
    "lint": "oxlint -c ../.oxlintrc.json ."
  },
  "dependencies": {
    "@react-email/render": "^2.1.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@react-email/ui": "^6.9.5",
    "react-email": "^6.9.5"
  }
}
