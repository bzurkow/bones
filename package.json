{
  "name": "bones",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "ui",
    "marketing"
  ],
  "scripts": {
    "test": "yarn workspace backend test",
    "install:all": "yarn --cwd backend install && yarn --cwd frontend install && yarn --cwd ui install && yarn --cwd marketing install",
    "initialize-backend": "sh scripts/initialize-backend.sh",
    "backend:dev": "NODE_VERSION=$(cat .node-version) docker compose up",
    "frontend:dev": "yarn workspace frontend dev",
    "marketing:dev": "yarn workspace marketing dev",
    "lint": "oxlint && syncpack lint",
    "lint:fix": "oxlint --fix && syncpack fix",
    "deps:check": "syncpack lint",
    "deps:fix": "syncpack fix"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^26.4.0",
    "oxlint": "^1.81.0",
    "syncpack": "^15.3.3",
    "typescript": "~7.0.2"
  }
}
