node_modules/
dist/
build/
storybook-static/
.env
.env.*
!.env.example
*.log
.DS_Store
*.tsbuildinfo

# web-static/ (Next.js)
.next/
next-env.d.ts

# design-sync (claude.ai/design import) - staged scripts, build output, and
# machine state are regenerated per run; config/NOTES/previews/overrides are
# the durable, committed set (see .design-sync/NOTES.md).
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
